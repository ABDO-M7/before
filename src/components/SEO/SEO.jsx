/**
 * SEO component – reserved for future use (e.g. per-page meta overrides).
 * Site-wide metadata is set in app/layout.js and per-route generateMetadata in pages.
 */
export default function SEO() {
  return null;
}
